# 🎨 ARCHITECTURE VISUALIZATION

## 📐 High-Level System Architecture

```
┌──────────────────────────────────────────────────────────────────┐
│                    COORDINATE MLP SYSTEM                         │
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│  Input: Grid[Batch, 5, H, W]                                   │
│    ↓                                                             │
│  ┌────────────────────────────────────────────────────────┐    │
│  │  1. COORDINATE GENERATOR                                │    │
│  │     • Normalize to [-1, 1]²                            │    │
│  │     • Generate (x,y) for each cell                     │    │
│  │     → Output: [H×W, 2]                                 │    │
│  └────────────────────────────────────────────────────────┘    │
│    ↓                                                             │
│  ┌────────────────────────────────────────────────────────┐    │
│  │  2. FOURIER POSITIONAL ENCODER                          │    │
│  │     • 6 frequency bands: [1, 2, 4, 8, 16, 32]         │    │
│  │     • sin/cos for each band                            │    │
│  │     → Output: [H×W, 26]                                │    │
│  └────────────────────────────────────────────────────────┘    │
│    ↓                                                             │
│  ┌────────────────────────────────────────────────────────┐    │
│  │  3. FEATURE COMBINATION                                 │    │
│  │     • Concat: Coords[26] + Grid[5]                     │    │
│  │     → Output: [Batch, H×W, 31]                         │    │
│  └────────────────────────────────────────────────────────┘    │
│    ↓                                                             │
│  ┌────────────────────────────────────────────────────────┐    │
│  │  4. CELL FEATURE MLP                                    │    │
│  │     Layer 1: Linear(31 → 512) + ReLU + LN             │    │
│  │     Layer 2: Linear(512 → 256) + ReLU + LN            │    │
│  │     Layer 3: Linear(256 → 256)                         │    │
│  │     → Output: [Batch, H×W, 256]                        │    │
│  └────────────────────────────────────────────────────────┘    │
│    ↓                                                             │
│  ┌────────────────────────────────────────────────────────┐    │
│  │  5. ATTENTION AGGREGATION                               │    │
│  │     Query: Learnable[1, 256]                           │    │
│  │     Keys/Values: Cell features[H×W, 256]               │    │
│  │     Multi-head (4 heads)                               │    │
│  │     → Output: [Batch, 256]                             │    │
│  └────────────────────────────────────────────────────────┘    │
│    ↓                                                             │
│  ┌────────────────────────────────────────────────────────┐    │
│  │  6. DUELING Q-NETWORK                                   │    │
│  │     Shared: Linear(256 → 256) + ReLU                   │    │
│  │     ┌─────────────┬─────────────┐                      │    │
│  │     Value         Advantage                             │    │
│  │     256→128→1     256→128→9                            │    │
│  │     └─────────────┴─────────────┘                      │    │
│  │     Combine: Q = V + (A - mean(A))                     │    │
│  │     → Output: [Batch, 9]                               │    │
│  └────────────────────────────────────────────────────────┘    │
│                                                                  │
│  Output: Q-values for 9 actions                                 │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Data Flow Example (20×20 Grid)

```
Step 1: Input Grid
┌─────────────────────────┐
│  [Batch=4, Channels=5,  │
│   Height=20, Width=20]  │
│                         │
│  400 cells total        │
└─────────────────────────┘
           ↓

Step 2: Generate Coordinates
┌─────────────────────────┐
│  [400, 2]               │
│                         │
│  (-1.0, -1.0)          │
│  (-0.9, -1.0)          │
│  ...                    │
│  (1.0, 1.0)            │
└─────────────────────────┘
           ↓

Step 3: Fourier Encoding
┌─────────────────────────┐
│  [400, 26]              │
│                         │
│  Each coord → 26D:      │
│  [x, y, sin(πx),       │
│   cos(πx), ...]        │
└─────────────────────────┘
           ↓

Step 4: Combine Features
┌─────────────────────────┐
│  [4, 400, 31]           │
│                         │
│  Concat:                │
│  Fourier[26] + Grid[5] │
└─────────────────────────┘
           ↓

Step 5: Cell Encoding
┌─────────────────────────┐
│  [4, 400, 256]          │
│                         │
│  Each cell independently│
│  processed by MLP       │
└─────────────────────────┘
           ↓

Step 6: Attention Pooling
┌─────────────────────────┐
│  [4, 256]               │
│                         │
│  Weighted aggregation   │
│  of 400 cells          │
└─────────────────────────┘
           ↓

Step 7: Q-Values
┌─────────────────────────┐
│  [4, 9]                 │
│                         │
│  Q-values for each      │
│  of 9 actions          │
└─────────────────────────┘
```

---

## 🧩 Component Hierarchy

```
CoordinateCoverageNetwork
├── FourierPositionalEncoding
│   ├── frequencies: [1, 2, 4, 8, 16, 32]
│   ├── output_dim: 26
│   └── forward(coords) → fourier_features
│
├── CellFeatureMLP
│   ├── Layer 1: Linear(31→512) + ReLU + Dropout + LN
│   ├── Layer 2: Linear(512→256) + ReLU + Dropout + LN
│   ├── Layer 3: Linear(256→256)
│   └── forward(combined_features) → cell_embeddings
│
├── AttentionPooling
│   ├── query: Parameter([1, 256])
│   ├── attention: MultiheadAttention(256, heads=4)
│   ├── output_mlp: [256→256→256]
│   └── forward(cell_embeddings) → aggregated_features
│
└── DuelingQNetwork
    ├── shared: Linear(256→256) + ReLU + Dropout
    ├── value_stream: [256→128→1]
    ├── advantage_stream: [256→128→9]
    └── forward(aggregated) → q_values
```

---

## 🎯 Training Loop Flow

```
┌───────────────────────────────────────────────────────┐
│                   TRAINING LOOP                       │
└───────────────────────────────────────────────────────┘
                         ↓
              ┌──────────────────┐
              │  Initialize Agent│
              │  • Policy Net    │
              │  • Target Net    │
              │  • Optimizer     │
              │  • Replay Buffer │
              └──────────────────┘
                         ↓
       ┌─────────────────────────────────────┐
       │  FOR episode = 1 to num_episodes   │
       └─────────────────────────────────────┘
                         ↓
              ┌──────────────────┐
              │  Sample Grid Size│
              │  [15,20,25,30]   │ ← Multi-scale curriculum
              └──────────────────┘
                         ↓
              ┌──────────────────┐
              │ Create Environment│
              │ with grid_size    │
              └──────────────────┘
                         ↓
       ┌─────────────────────────────────────┐
       │  WHILE not done:                    │
       │    1. Select action (ε-greedy)      │
       │    2. Execute in environment        │
       │    3. Store transition              │
       │    4. Sample batch from buffer      │
       │    5. Compute TD targets            │
       │    6. Update policy network         │
       │    7. Update target network (soft)  │
       └─────────────────────────────────────┘
                         ↓
              ┌──────────────────┐
              │  Decay Epsilon   │
              │  ε *= 0.995      │
              └──────────────────┘
                         ↓
              ┌──────────────────┐
              │  Log Metrics     │
              │  Save Checkpoint │
              └──────────────────┘
                         ↓
       ┌─────────────────────────────────────┐
       │  END FOR                            │
       └─────────────────────────────────────┘
```

---

## 🧪 Testing Flow

```
┌───────────────────────────────────────────────────────┐
│               SCALE INVARIANCE TEST                   │
└───────────────────────────────────────────────────────┘
                         ↓
              ┌──────────────────┐
              │  Load Checkpoint │
              │  (trained model) │
              └──────────────────┘
                         ↓
       ┌─────────────────────────────────────┐
       │  FOR size in [20,25,30,35,40,50]:  │
       └─────────────────────────────────────┘
                         ↓
       ┌─────────────────────────────────────┐
       │  FOR episode = 1 to 20:             │
       │    1. Create environment(size)      │
       │    2. Run greedy policy (ε=0)       │
       │    3. Record coverage metrics       │
       └─────────────────────────────────────┘
                         ↓
              ┌──────────────────┐
              │ Compute Stats    │
              │ • Mean coverage  │
              │ • Std deviation  │
              │ • Degradation    │
              └──────────────────┘
                         ↓
       ┌─────────────────────────────────────┐
       │  END FOR                            │
       └─────────────────────────────────────┘
                         ↓
              ┌──────────────────┐
              │  Generate Report │
              │  • Tables        │
              │  • Plots         │
              │  • JSON results  │
              └──────────────────┘
```

---

## 🔍 Attention Mechanism Detail

```
┌──────────────────────────────────────────────────────┐
│           MULTI-HEAD ATTENTION POOLING               │
├──────────────────────────────────────────────────────┤
│                                                      │
│  Input: Cell Features [Batch, H×W, 256]            │
│                                                      │
│  ┌────────────────────────────────────────────┐    │
│  │  Query (Learnable)                          │    │
│  │  [1, 256] → expanded to [Batch, 1, 256]    │    │
│  └────────────────────────────────────────────┘    │
│                    ↓                                │
│  ┌────────────────────────────────────────────┐    │
│  │  Multi-Head Attention (4 heads)            │    │
│  │                                              │    │
│  │  For each head:                             │    │
│  │    • Q = query_projection                   │    │
│  │    • K = key_projection(cell_features)      │    │
│  │    • V = value_projection(cell_features)    │    │
│  │    • Attention = softmax(QK^T / √d_k)       │    │
│  │    • Output = Attention × V                 │    │
│  └────────────────────────────────────────────┘    │
│                    ↓                                │
│  ┌────────────────────────────────────────────┐    │
│  │  Concatenate heads                          │    │
│  │  [Batch, 1, 256]                           │    │
│  └────────────────────────────────────────────┘    │
│                    ↓                                │
│  ┌────────────────────────────────────────────┐    │
│  │  Squeeze & Post-process                     │    │
│  │  Linear(256→256) + ReLU + Linear(256→256)  │    │
│  └────────────────────────────────────────────┘    │
│                    ↓                                │
│  Output: Aggregated Features [Batch, 256]          │
│                                                      │
└──────────────────────────────────────────────────────┘

Example Attention Weights (20×20 grid):

Cell (0,0)   : 0.001  ← Low attention (corner)
Cell (10,5)  : 0.152  ← High attention (frontier)
Cell (12,8)  : 0.243  ← Highest (agent position)
Cell (19,19) : 0.0002 ← Very low (already covered)
...
Sum = 1.0 (normalized distribution)
```

---

## 📊 Parameter Count Breakdown

```
┌─────────────────────────────────────────────────────┐
│              MODEL PARAMETERS                       │
├─────────────────────────────────────────────────────┤
│                                                     │
│  1. FourierPositionalEncoding                      │
│     • No trainable parameters (fixed frequencies)  │
│     Parameters: 0                                  │
│                                                     │
│  2. CellFeatureMLP                                 │
│     • Layer 1: 31 × 512 + 512 = 16,384            │
│     • LN 1:    512 × 2 = 1,024                    │
│     • Layer 2: 512 × 256 + 256 = 131,328         │
│     • LN 2:    256 × 2 = 512                      │
│     • Layer 3: 256 × 256 + 256 = 65,792          │
│     Subtotal: ~215K parameters                     │
│                                                     │
│  3. AttentionPooling                               │
│     • Query:   256 = 256                           │
│     • MHA:     256 × 256 × 3 × 4 + bias ≈ 787K   │
│     • Out MLP: 256×256 + 256×256 ≈ 131K          │
│     Subtotal: ~918K parameters                     │
│                                                     │
│  4. DuelingQNetwork                                │
│     • Shared:     256 × 256 + 256 = 65,792        │
│     • Value:      256 × 128 + 128 × 1 ≈ 33K      │
│     • Advantage:  256 × 128 + 128 × 9 ≈ 34K      │
│     Subtotal: ~133K parameters                     │
│                                                     │
├─────────────────────────────────────────────────────┤
│  TOTAL: ~1.3M parameters                           │
│  (Optimized model: ~400K with smaller attention)   │
└─────────────────────────────────────────────────────┘
```

---

## 🎨 Color-Coded Information Flow

```
🔵 Input Grid Data
    ↓
🟢 Coordinate Processing (Scale-Invariant)
    ↓
🟡 Feature Extraction (Per-Cell Independent)
    ↓
🟠 Attention Aggregation (Spatial Importance)
    ↓
🔴 Q-Value Prediction (Action Selection)
```

---

This architecture enables **scale invariance** by:
1. ✅ Normalizing coordinates to fixed range
2. ✅ Using position-aware features (Fourier)
3. ✅ Processing cells independently (no fixed receptive field)
4. ✅ Learning adaptive attention patterns
5. ✅ Training on multiple scales simultaneously
